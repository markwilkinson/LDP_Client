source "https://rubygems.org"

git_source(:github) {|repo_name| puts "https://github.com/#{repo_name}"; return "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in ldp.gemspec
gemspec
